import React, { useEffect } from 'react';

const AddedItems = () => {

  useEffect(() => {
    window.scrollTo(0, 0)
  },[])
  return (
    <div>
      Added i
    </div>
  );
};

export default AddedItems;